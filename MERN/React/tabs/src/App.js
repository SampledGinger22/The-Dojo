import logo from './logo.svg';
import './App.css';

function App(arr = []) {

  const ArrHandler = (e, array) => {

  }

  return (
    <div className="App">
      <div className='flexrow'>
      { arr.map( (string, i) => 
        <
      ) }
      </div>

    </div>
  );
}

export default App;
